name: release
on:
  push:
    branches:
      - main
  workflow_dispatch:
concurrency:
  cancel-in-progress: false
  group: ${{ github.workflow }}-${{ github.ref }}
jobs:
  release:
    env:
      FORCE_COLOR: 1
      UV_FROZEN: 1
      UV_NO_SYNC: 1
      UV_PYTHON_PREFERENCE: only-managed
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
      - name: Install dependencies
        run: uv sync --only-group release
      - name: Detect release
        id: release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "prev_tag=$(uv run semantic-release version --print-last-released-tag)" >> "$GITHUB_OUTPUT"
          echo "next_tag=$(uv run semantic-release version --print-tag)" >> "$GITHUB_OUTPUT"
      - name: Create release
        if: steps.release.outputs.prev_tag != steps.release.outputs.next_tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          uv run semantic-release version --no-changelog --no-commit --push --skip-build
          uv build
          uv run semantic-release publish --tag '${{ steps.release.outputs.next_tag }}'
